/* function move() {
    var elem = document.getElementsByTagName("progress-bar");   
    var width = 1;
    var class = setInterval(frame, 10);
    function frame() {
      if (width >= 90) {
        clearInterval(class);
      } else {
        width++; 
        elem.style.width = width + '%'; 
      }
    }
  } */